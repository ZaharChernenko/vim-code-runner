repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: bash -c "cd python_coderunner && ruff check --fix ."
        language: system
        types: [python]
        stages: [pre-commit]
        verbose: true

      - id: ruff_format
        name: ruff_format
        # not in args, because entry is bash
        entry: bash -c "cd python_coderunner && ruff format --quiet ."
        language: system
        types: [python]
        stages: [pre-commit]
        verbose: true

      - id: pyright
        name: pyright
        entry: bash -c "cd python_coderunner && pyright ."
        language: system
        types: [python]
        stages: [pre-commit]
        verbose: true

      - id: pylint
        name: pylint
        entry: bash -c "cd python_coderunner && pylint ."
        language: system
        types: [python]
        stages: [pre-commit]
        verbose: true

      - id: commitlint
        name: commitlint
        entry: commitlint
        args: [--file]
        language: system
        stages: [commit-msg]
        verbose: true
